services:
  - type: web
    name: ngai-platform
    env: docker
    plan: free
    dockerCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PUTER_API_KEY
        sync: false
